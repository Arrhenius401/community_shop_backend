<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.graygoo401.community.dao.mapper.PostFollowMapper">

    <!-- 基础字段SQL片段 -->
    <sql id="Base_Column_List">
        post_follow_id, post_id, user_id, parent_id, content, `status`, create_time, update_time
    </sql>

    <!-- ==================== 关联查询 ==================== -->
    <select id="selectByPostId" resultType="xyz.graygoo401.community.dao.entity.PostFollow">
        SELECT <include refid="Base_Column_List"/> FROM post_follow
        WHERE post_id = #{postId} AND status = 'NORMAL'
        ORDER BY create_time ASC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="selectByUserId" resultType="xyz.graygoo401.community.dao.entity.PostFollow">
        SELECT <include refid="Base_Column_List"/> FROM post_follow
        WHERE user_id = #{userId} AND status = 'NORMAL'
        ORDER BY create_time ASC
    </select>

    <select id="selectByParentId" resultType="xyz.graygoo401.community.dao.entity.PostFollow">
        SELECT <include refid="Base_Column_List"/> FROM post_follow
        WHERE parent_id = #{parentId} AND status = 'NORMAL'
        ORDER BY create_time ASC
    </select>

    <!-- 按跟帖 查询条件统计跟帖数量 -->
    <select id="countByQuery" resultType="int">
        SELECT COUNT(*) FROM post_follow
        <where>
            <!-- 帖子ID搜索 -->
            <if test="postId != null">
                AND post_id = #{post_id} <!-- 简化：无需显式指定typeHandler -->
            </if>

            <if test="status != null and status != ''">
                AND status = #{status.code,jdbcType=VARCHAR} <!-- 简化：无需显式指定typeHandler -->
            </if>

        </where>
    </select>

    <!-- 按跟帖查询条件分页查询跟帖 -->
    <select id="selectByQuery" resultType="xyz.graygoo401.community.dao.entity.PostFollow">
        SELECT <include refid="Base_Column_List"/> FROM post_follow
        <where>
            <!-- 帖子ID搜索 -->
            <if test="query.postId != null">
                AND post_id = #{query.postId} <!-- 简化：无需显式指定typeHandler -->
            </if>

            <if test="query.status != null">
                AND status = #{query.status.code,jdbcType=VARCHAR} <!-- 简化：无需显式指定typeHandler -->
            </if>
        </where>

        <!-- 排序：支持用户常用的排序方式 -->
        <if test="query.sortField != null and query.sortDir != null">
            ORDER BY ${query.sortField} ${query.sortDir}
        </if>
        <if test="query.sortField == null">
            <!-- 默认按更新时间倒序 -->
            ORDER BY create_time ASC
        </if>

        <!-- 分页参数 -->
        <if test="query.pageNum != null and query.pageSize != null">
            LIMIT #{query.offset}, #{query.pageSize}
        </if>
    </select>

    <!-- ==================== 统计功能 ==================== -->
    <select id="countByPostId" resultType="int">
        SELECT COUNT(*) FROM post_follow
        WHERE post_id = #{postId} AND status = 'NORMAL'
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM post_follow
        WHERE user_id = #{userId} AND status = 'NORMAL'
    </select>


    <!-- ==================== 批量操作 ==================== -->
    <update id="batchDeleteByPostId">
        UPDATE post_follow SET status = 'DELETED' WHERE post_id = #{postId}
    </update>

    <update id="batchDeleteByUserId">
        UPDATE post_follow SET status = 'DELETED' WHERE user_id = #{userId}
    </update>

</mapper>