<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.graygoo401.community.dao.mapper.UserPostLikeMapper">

    <!-- 基础字段SQL片段 -->
    <sql id="Base_Column_List">
        like_id, user_id, post_id, status, create_time
    </sql>


    <!-- ==================== 基础操作 ==================== -->

    <delete id="deleteByUserAndPost">
        UPDATE user_post_like
        SET status = 'CANCELLED'
        WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>

    <select id="selectByUserAndPost" resultType="xyz.graygoo401.community.dao.entity.UserPostLike">
        SELECT <include refid="Base_Column_List"/> FROM user_post_like
        WHERE user_id = #{userId} AND post_id = #{postId}
        AND status = 'ACTIVE'
    </select>


    <!-- ==================== 统计与列表查询 ==================== -->
    <select id="countByPostId" resultType="int">
        SELECT COUNT(*) FROM user_post_like
        WHERE post_id = #{postId} AND status = 'ACTIVE'
    </select>

    <select id="selectPostIdsByUserId" resultType="java.lang.Long">
        SELECT post_id FROM user_post_like
        WHERE user_id = #{userId} AND status = 'ACTIVE'
        ORDER BY create_time DESC
    </select>

    <select id="selectUserIdsByPostId" resultType="java.lang.Long">
        SELECT user_id FROM user_post_like
        WHERE post_id = #{postId} AND status = 'ACTIVE'
        ORDER BY create_time DESC
            LIMIT #{limit}
    </select>


    <!-- ==================== 批量操作 ==================== -->
    <update id="batchDeleteByPostId">
        UPDATE user_post_like
        SET status = 'CANCELLED'
        WHERE post_id = #{postId}
    </update>

    <update id="batchDeleteByUserId">
        UPDATE user_post_like
        SET status = 'CANCELLED'
        WHERE user_id = #{userId}
    </update>

</mapper>