<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community_shop.backend.mapper.UserMapper">

    <!-- 结果集映射：匹配User实体与user表字段 -->
    <resultMap id="BaseResultMap" type="com.community_shop.entity.User">
        <id column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="email" property="email"/>
        <result column="phone_number" property="phoneNumber"/>
        <result column="password" property="password"/>
        <result column="avatar_url" property="avatarUrl"/>
        <result column="bio" property="bio"/>
        <!-- 枚举类型映射：通过code匹配数据库varchar字段 -->
        <result column="gender" property="gender" typeHandler="com.community_shop.handler.GenderEnumTypeHandler"/>
        <result column="role" property="role" typeHandler="com.community_shop.handler.UserRoleEnumTypeHandler"/>
        <result column="credit_score" property="creditScore"/>
        <result column="post_count" property="postCount"/>
        <result column="follower_count" property="followerCount"/>
        <result column="interest_tags" property="interestTags" typeHandler="com.community_shop.handler.ListStringTypeHandler"/>
        <result column="activity_time" property="activityTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 基础字段SQL片段，减少冗余 -->
    <sql id="Base_Column_List">
        user_id, username, email, phone_number, password, avatar_url, bio, gender, role,
        credit_score, post_count, follower_count, interest_tags, activity_time, create_time
    </sql>


    <!-- ==================== 基础CRUD ==================== -->
    <insert id="insert" parameterType="com.community_shop.entity.User">
        INSERT INTO user (
            username, email, phone_number, password, avatar_url, bio, gender, role,
            credit_score, post_count, follower_count, interest_tags, activity_time, create_time
        ) VALUES (
                     #{username}, #{email}, #{phoneNumber}, #{password}, #{avatarUrl}, #{bio},
                     #{gender.code,jdbcType=VARCHAR,typeHandler=com.community_shop.handler.GenderEnumTypeHandler},
                     #{role.code,jdbcType=VARCHAR,typeHandler=com.community_shop.handler.UserRoleEnumTypeHandler},
                     #{creditScore}, #{postCount}, #{followerCount},
                     #{interestTags,jdbcType=VARCHAR,typeHandler=com.community_shop.handler.ListStringTypeHandler},
                     #{activityTime}, #{createTime}
                 )
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM user WHERE user_id = #{userId}
    </select>

    <update id="updateById" parameterType="com.community_shop.entity.User">
        UPDATE user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phoneNumber != null">phone_number = #{phoneNumber},</if>
            <if test="password != null">password = #{password},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="bio != null">bio = #{bio},</if>
            <if test="gender != null">
                gender = #{gender.code,jdbcType=VARCHAR,typeHandler=com.community_shop.handler.GenderEnumTypeHandler},
            </if>
            <if test="role != null">
                role = #{role.code,jdbcType=VARCHAR,typeHandler=com.community_shop.handler.UserRoleEnumTypeHandler},
            </if>
            <if test="activityTime != null">activity_time = #{activityTime},</if>
        </set>
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE user_id = #{userId}
    </delete>


    <!-- ==================== 登录与身份校验 ==================== -->
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM user WHERE username = #{username}
    </select>

    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM user WHERE phone_number = #{phoneNumber}
    </select>

    <select id="selectByEmail" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM user WHERE email = #{email}
    </select>


    <!-- ==================== 信用分与统计 ==================== -->
    <update id="updateCreditScore">
        UPDATE user SET credit_score = #{score} WHERE user_id = #{userId}
    </update>

    <update id="updatePostCount">
        UPDATE user SET post_count = post_count + 1 WHERE user_id = #{userId}
    </update>

    <update id="updateFollowerCount">
        UPDATE user SET follower_count = #{count} WHERE user_id = #{userId}
    </update>


    <!-- ==================== 条件查询 ==================== -->
    <select id="selectByInterestTags" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM user
        WHERE FIND_IN_SET(interest_tags, #{tags,typeHandler=com.community_shop.handler.ListStringTypeHandler})
    </select>

    <select id="getUsersByAllParam" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM user
        <where>
            <if test="compareIndex != null and compareParam != null">
                ${compareIndex} = #{compareParam}
            </if>
        </where>
        <if test="order != null and direction != null">
            ORDER BY ${order} ${direction}
        </if>
        LIMIT #{offset}, #{limit}
    </select>

    <select id="selectByRole" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM user
        WHERE role = #{role.code,jdbcType=VARCHAR,typeHandler=com.community_shop.handler.UserRoleEnumTypeHandler}
    </select>

</mapper>